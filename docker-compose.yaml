services:
  mana:
    build:
      context: .
    image: mana:local
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      MENU_PATH: /app/content/menu.yaml
    volumes:
      # Mount the directory so atomic file replacements by editors remain visible.
      - ./content:/app/content:ro
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
